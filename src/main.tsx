import React from 'react';
import ReactDOM from 'react-dom/client';
import UitspraakZinnenApp from './UitspraakZinnenApp';
import './style.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <UitspraakZinnenApp />
  </React.StrictMode>
);
